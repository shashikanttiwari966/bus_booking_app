<div class="m-4">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand text-dark navbar-title" href="#">TechBus</a>
      <!-- <a href="#" class="navbar-brand">
          <img src="/examples/images/logo.svg" height="28" alt="CoolBrand">
      </a> -->
      <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav">
            <%= link_to root_path(), class:"nav-item nav-link active" do%>
              <i class="fa fa-home" aria-hidden="true"></i> Home
            <%end%>
            <% if user_signed_in?%>
                <%= link_to buses_path(), class:"nav-item nav-link" do%>
                  <i class="fa fa-bus" aria-hidden="true"></i> Buses
                <%end%>
            <%end%>
        </div>
        <div class="navbar-nav ms-auto">
          <% if user_signed_in?%>
            <%= link_to destroy_user_session_path(), method: :delete, class:"nav-item nav-link" do%>
            Logout <i class="fa fa-sign-out" aria-hidden="true"></i>
            <span class="p-2"><i class="fa fa-globe" aria-hidden="true"></i> (<%= current_user.service_state%>)</span>
            <%end%>
          <%else%>
             <%= link_to "Login", new_user_session_path(), class:"nav-item nav-link"%>
             <%= link_to "SignUp", new_user_registration_path(), class:"nav-item nav-link"%>
          <%end%>
        </div>
      </div>
    </div>
  </nav>
</div>
<%#= form_tag("/", :method => "get", class:"form-inline d-flex") do %>
  <%#= text_field_tag :search,'',placeholder:"Search",class: "form-control mr-2" %>
  <%#= submit_tag "Search", class:"btn btn-outline-success" %>
<%# end %>